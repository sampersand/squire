#!../squire -f
@henceforth $bug($msg) = catapult($msg) ;

@transcribe "env.sq"
@transcribe "functions.sq"
@transcribe "parser.sq"
#@transcribe "value.sq"

#@transcribe "value.sq"
#ARGV[1]=1;
#proclaim("a\(ARGV)")



p = Parser(ARGV[II]);
p.next().run()
#proclaim(p.take_whilst((journey(c) => '0' <= c && c <= '9')));
