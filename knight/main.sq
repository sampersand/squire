#!../squire -f
@henceforth $bug($msg) = catapult($msg) ;

@transcribe "env.sq"
@transcribe "functions.sq"
@transcribe "parser.sq"
#@transcribe "value.sq"

#@transcribe "value.sq"
#ARGV[1]=1;
#proclaim("a\(ARGV)")



Variable.lookup("v").assign(34);
p = Parser("  #lol
	; = x 3
	: DUMP @+ x 5
	x");
p.next().run()
#proclaim(p.take_whilst((journey(c) => '0' <= c && c <= '9')));
