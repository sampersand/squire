# Basic fizzbuzz, nothing fancy.

i = I
whilst i <= C {
	mod-three = i % III
	mod-five = i % V

	if mod-three && mod-five {
		proclaim(arabic(i))
	} alas if mod-three {
		proclaim('Fizz')
	} alas if mod-five {
		proclaim('Buzz')
	} alas {
		proclaim('FizzBuzz')
	}

	i = i + I
}
