# Basic fizzbuzz, nothing fancy.

i = I
whilst i <= C {
	mod-three = i % III
	mod-five = i % V

	if mod-three && mod-five {
		proclaim(arabic(i))
	} alas if mod-three {
		proclaim(𝔉𝔦𝔷𝔷𝔅𝔲𝔷𝔷)
	} alas if mod-five {
		proclaim(𝔅𝔲𝔷𝔷)
	} alas {
		proclaim(𝔉𝔦𝔷𝔷𝔅𝔲𝔷𝔷)
	}

	i = i + I
}
