import "linkedlist.sq"

journey timestr(time) {
	if time % 2 { minutes = "30" }
	else { minutes = "00"};

	rawhours = time / 2;

	if (rawhours % 12) { hours = string(rawhours % 12) }
	else { hours = "12"};

	if rawhours >= 12 { ampm = "pm" }
	else { ampm = "am" }

	reward hours + ":" + minutes + " " + ampm
}


journey shared_times(usera, userb) {
	shared = empty()

	i = 0
}

usera = cons(1, cons(3, cons(9, empty())))
#dump(share(foo, 1))