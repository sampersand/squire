@henceforth $divides($numer) = (journey(n) => !(n % $numer));

i = I
whilst i <= C {
	fork i {
		path $divides(XV):  proclaim('FizzBuzz')
		path $divides(III): proclaim('Fizz')
		path $divides(V):   proclaim('Buzz')
		alas:               proclaim(i)
	}

	i = i + I
}
