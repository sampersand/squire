foo:
proclaim("Never run\n");

whence foo
proclaim("Spot I\n");

whence foo
proclaim("Spot II\n");

whence foo
proclaim("Spot III\n");

__END__
i = I
whilst i < XL {
	proclaim("\(i)\n")

	if i >= X {
		more_than_ten:
		#...;
	} alas {
		i = i + I
	}
}

whence more_than_ten
proclaim("done\n")
